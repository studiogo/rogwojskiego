name: Deploy to WordPress

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🔄 Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 2
    
    - name: 📤 FTP Deploy
      uses: SamKirkland/FTP-Deploy-Action@v4.3.6
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        dangerous-clean-slate: false  # NIE usuwaj wszystkiego
        dry-run: false
        dangerous-clean-slate: false  # ❗ WAŻNE - nie resetuj historii
        protocol: ftps  # Spróbuj najpierw FTPS
        port: 21
        server-dir: /
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.vscode/**
          **/.DS_Store
          **/README.md
          **/upload-to-ftp.*
          **/.github/**
        log-level: verbose
        security: loose  # Mniej restrykcyjne sprawdzanie certyfikatu
        timeout: 6300000  # 5 minut timeout
    
    - name: ✅ Deployment completed
      if: success()
      run: |
        echo "✅ Pliki WordPress zostały wgrane na serwer!"
        echo "🌐 Sprawdź stronę: https://torty.studiogo2.kylos.pl"
    
    - name: ❌ Deployment failed
      if: failure()
      run: |
        echo "❌ Deployment nie powiódł się. Sprawdź logi powyżej."
